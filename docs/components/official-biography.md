# OfficialBiography

A full official-profile page pattern: gradient header with photo, contact
information card, education timeline, biography paragraphs, achievements and
optional call-to-action buttons.

<DemoBlock name="official-biography" demo="default" />

## Framework notes

- **React** — identical with `class` → `className`; feed profile data from a single `official` object.
- **Vue / Svelte** — markup works as-is; bind profile fields with interpolation.
