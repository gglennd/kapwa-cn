# CivicTechBanner

A large full-width campaign banner on a warm gradient, with a headline, a
highlighted word, description, and two CTA anchors.

<DemoBlock name="civic-tech-banner" demo="default" />

## Framework notes

- **React / Vue / Svelte** — stateless anchors; markup works unchanged (React needs `class` → `className`).
