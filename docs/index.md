---
layout: home

hero:
  name: kapwa-cn
  text: Components for Philippine government portals
  tagline: Copy a snippet, paste it, ship it. Every component is plain HTML styled with the Kapwa design tokens, so it renders identically in React, Vue, Svelte or a static page — no build step, no framework lock-in.
  actions:
    - theme: brand
      text: Browse components
      link: /components/button
    - theme: alt
      text: Getting started
      link: /guides/getting-started

features:
  - title: Copy-paste ready
    details: See each variant live, expand the markup, and copy the exact snippet you need — not a whole library to learn.
  - title: Plain HTML
    details: No custom elements, no shadow DOM, no build step. What you copy is valid markup that works anywhere HTML runs.
  - title: Framework-native
    details: Interactivity stays in your framework's own state — useState, ref, signals — while the markup stays plain and out of the way.
  - title: Kapwa tokens
    details: The same CSS custom properties and Tailwind v4 theme as the React design system, so visuals stay identical everywhere.
---
