---
layout: home

hero:
  name: kapwa-cn
  text: Framework-agnostic components for Philippine government portals
  tagline: Copy the markup, paste it, done. Every component is plain HTML styled with the Kapwa design tokens, so it renders identically in React, Vue, Svelte or a static page.
  actions:
    - theme: brand
      text: Browse components
      link: /components/button
    - theme: alt
      text: Getting started
      link: /guides/getting-started

features:
  - title: Plain HTML
    details: No custom elements, no shadow DOM, no build step. What you copy is valid markup that works anywhere HTML runs.
  - title: Framework-native
    details: Interactivity stays in your framework's own state — useState, ref, signals. The markup stays plain and out of the way.
  - title: Kapwa tokens
    details: The same CSS custom properties and Tailwind v4 theme as the React design system, keeping visuals identical everywhere.
  - title: Copy-paste ready
    details: See each variant live, expand the markup, and copy the exact snippet you need.
---
